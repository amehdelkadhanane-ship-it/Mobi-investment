window.CONTIX_CONFIG = {
  // Leave blank when frontend and backend are served from the same domain.
  // For GitHub Pages + separate backend, set this to the backend HTTPS origin.
  API_BASE_URL: ""
};
